
import styles from './Gradient.module.css';

const GradientCircle = () => {
    return (
        <div className={styles['gradient-circle']} />
        // <div className={`radial-gradient w-full h-full bg-${color}`} />
    );
};

export default GradientCircle;
